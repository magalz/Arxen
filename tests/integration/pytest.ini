[pytest]
minversion = 9.1
testpaths = .
addopts =
    --strict-config
    --strict-markers
    --import-mode=importlib
    --no-cov
    --junitxml=test-results/integration/junit.xml
markers =
    integration: requires a real PostgreSQL database with pgvector available
